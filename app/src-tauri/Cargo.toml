[package]
name = "editor-app"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
sysinfo = "0.33"
serde = { version = "1", features = ["derive"] }
