{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "WebView は editor_server が配信する http://127.0.0.1:<動的ポート> から読まれる。この remote 元から自作コマンドと dialog (フォルダ選択) を invoke できるようにする。",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "permissions": ["core:default", "dialog:allow-open"]
}
